/**
 * Port Butler 文件说明：
 * Port Butler 版本常量。
 * CLI 和 TUI 使用同一份版本号，避免不同界面显示不一致。
 * 发布流程更新版本时只需要维护这个公共常量。
 */
/**
 * Port Butler 当前发布版本。CLI 与 TUI 都从这里读取展示版本，避免硬编码分散。
 */
export const PORT_BUTLER_VERSION = "0.2.0";
